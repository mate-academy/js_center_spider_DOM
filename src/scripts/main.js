'use strict';

// const wall = document.querySelector('.wall');
const spider = document.querySelector('.spider');
// const w = wall.style.width.replace('px', '') / 2
// - spider.style.width.replace('px', '') / 2;
// const h = wall.style.height / 2 - spider.style.height / 2;

// console.log(wall.style);

// spider.style.top = `${h}px`;
// spider.style.left = `${w}px`;

spider.style.top = `175px`;
spider.style.left = `175px`;
