'use strict';

const spider = document.querySelector('.spider');

const styles = {
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
};

Object.assign(spider.style, styles);
