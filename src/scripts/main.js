'use strict';

const spiderHTML = document.querySelector('.spider');

spiderHTML.style.position = 'relative';
spiderHTML.style.top = '50%';
spiderHTML.style.left = '50%';
spiderHTML.style.transform = 'translate(-50%, -50%)';
